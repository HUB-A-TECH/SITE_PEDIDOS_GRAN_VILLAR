{
  "name": "gran-villar",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema de Pedidos Gran Villar — build unificado para deploy (Render).",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install --include=dev && npx prisma generate && npm run build",
    "start": "cd backend && npx prisma migrate deploy && node dist/server.js"
  }
}
